name: receipt_claim_flutter_web
description: Screenless smart fridge receipt claim portal built with Flutter web.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  intl: ^0.19.0
  url_launcher: ^6.3.0

flutter:
  uses-material-design: true
